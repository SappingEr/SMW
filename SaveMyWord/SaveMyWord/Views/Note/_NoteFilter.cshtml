@model SaveMyWord.Models.Filters.NoteFilter

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="input-group">
        <div>
            @Html.EditorFor(model => model.SearchString, new { htmlAttributes = new { @class = "form-control", style = "max-width:none;" } })
        </div>
        <div class="input-group-btn">
            <input type="submit" value="Найти" class="btn btn-primary" />
        </div>
    </div>
}

